import { IonContent, IonHeader, IonPage, IonTitle } from '@ionic/react';


const ChargePointNotSelected: React.FC = (props) => {
  return (
    <IonPage>
      <IonHeader>
        <IonTitle>No Charge Point Selected</IonTitle>
      </IonHeader>
      <IonContent>
      </IonContent>
    </IonPage>
  );
};

export default ChargePointNotSelected;